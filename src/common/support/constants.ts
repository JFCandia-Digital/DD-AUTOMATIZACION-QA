import * as path from 'path';

export const PATHS = {
  FILES_DIRECTORY: path.join(process.cwd(), 'src', 'data', 'files'),
  JSON_HISTORICS_DIRECTORY: path.join(process.cwd(), 'reports', 'json-acuse-recibo'),
};